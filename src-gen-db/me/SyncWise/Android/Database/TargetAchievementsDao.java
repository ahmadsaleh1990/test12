package me.SyncWise.Android.Database;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.DaoConfig;
import de.greenrobot.dao.Property;

import me.SyncWise.Android.Database.TargetAchievements;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table TargetAchievements.
*/
public class TargetAchievementsDao extends AbstractDao<TargetAchievements, Long> {

    public static final String TABLENAME = "TargetAchievements";

    /**
     * Properties of entity TargetAchievements.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property TargetCode = new Property(1, String.class, "TargetCode", false, "TargetCode");
        public final static Property LineID = new Property(2, int.class, "LineID", false, "LineID");
        public final static Property SubjectCode = new Property(3, String.class, "SubjectCode", false, "SubjectCode");
        public final static Property TargetDetailType = new Property(4, Integer.class, "TargetDetailType", false, "TargetDetailType");
        public final static Property CompanyCode = new Property(5, String.class, "CompanyCode", false, "CompanyCode");
        public final static Property DivisionCode = new Property(6, String.class, "DivisionCode", false, "DivisionCode");
        public final static Property AmountAcheived = new Property(7, Double.class, "AmountAcheived", false, "AmountAcheived");
        public final static Property StampDate = new Property(8, java.util.Date.class, "StampDate", false, "StampDate");
    };


    public TargetAchievementsDao(DaoConfig config) {
        super(config);
    }
    
    public TargetAchievementsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'TargetAchievements' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'TargetCode' TEXT NOT NULL ," + // 1: TargetCode
                "'LineID' INTEGER NOT NULL ," + // 2: LineID
                "'SubjectCode' TEXT NOT NULL ," + // 3: SubjectCode
                "'TargetDetailType' INTEGER," + // 4: TargetDetailType
                "'CompanyCode' TEXT NOT NULL ," + // 5: CompanyCode
                "'DivisionCode' TEXT NOT NULL ," + // 6: DivisionCode
                "'AmountAcheived' REAL," + // 7: AmountAcheived
                "'StampDate' INTEGER NOT NULL );"); // 8: StampDate
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'TargetAchievements'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, TargetAchievements entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getTargetCode());
        stmt.bindLong(3, entity.getLineID());
        stmt.bindString(4, entity.getSubjectCode());
 
        Integer TargetDetailType = entity.getTargetDetailType();
        if (TargetDetailType != null) {
            stmt.bindLong(5, TargetDetailType);
        }
        stmt.bindString(6, entity.getCompanyCode());
        stmt.bindString(7, entity.getDivisionCode());
 
        Double AmountAcheived = entity.getAmountAcheived();
        if (AmountAcheived != null) {
            stmt.bindDouble(8, AmountAcheived);
        }
        stmt.bindLong(9, entity.getStampDate().getTime());
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public TargetAchievements readEntity(Cursor cursor, int offset) {
        TargetAchievements entity = new TargetAchievements( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // TargetCode
            cursor.getInt(offset + 2), // LineID
            cursor.getString(offset + 3), // SubjectCode
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // TargetDetailType
            cursor.getString(offset + 5), // CompanyCode
            cursor.getString(offset + 6), // DivisionCode
            cursor.isNull(offset + 7) ? null : cursor.getDouble(offset + 7), // AmountAcheived
            new java.util.Date(cursor.getLong(offset + 8)) // StampDate
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, TargetAchievements entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setTargetCode(cursor.getString(offset + 1));
        entity.setLineID(cursor.getInt(offset + 2));
        entity.setSubjectCode(cursor.getString(offset + 3));
        entity.setTargetDetailType(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setCompanyCode(cursor.getString(offset + 5));
        entity.setDivisionCode(cursor.getString(offset + 6));
        entity.setAmountAcheived(cursor.isNull(offset + 7) ? null : cursor.getDouble(offset + 7));
        entity.setStampDate(new java.util.Date(cursor.getLong(offset + 8)));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(TargetAchievements entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(TargetAchievements entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
