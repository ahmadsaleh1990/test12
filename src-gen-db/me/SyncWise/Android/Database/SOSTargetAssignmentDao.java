package me.SyncWise.Android.Database;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.DaoConfig;
import de.greenrobot.dao.Property;

import me.SyncWise.Android.Database.SOSTargetAssignment;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table SOSTargetAssignment.
*/
public class SOSTargetAssignmentDao extends AbstractDao<SOSTargetAssignment, Long> {

    public static final String TABLENAME = "SOSTargetAssignment";

    /**
     * Properties of entity SOSTargetAssignment.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property AssignmentCode = new Property(1, String.class, "AssignmentCode", false, "AssignmentCode");
        public final static Property TargetCode = new Property(2, String.class, "TargetCode", false, "TargetCode");
        public final static Property CompanyCode = new Property(3, String.class, "CompanyCode", false, "CompanyCode");
        public final static Property AssignmentType = new Property(4, Integer.class, "AssignmentType", false, "AssignmentType");
        public final static Property StampDate = new Property(5, java.util.Date.class, "StampDate", false, "StampDate");
    };


    public SOSTargetAssignmentDao(DaoConfig config) {
        super(config);
    }
    
    public SOSTargetAssignmentDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'SOSTargetAssignment' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'AssignmentCode' TEXT," + // 1: AssignmentCode
                "'TargetCode' TEXT," + // 2: TargetCode
                "'CompanyCode' TEXT," + // 3: CompanyCode
                "'AssignmentType' INTEGER," + // 4: AssignmentType
                "'StampDate' INTEGER NOT NULL );"); // 5: StampDate
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'SOSTargetAssignment'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, SOSTargetAssignment entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String AssignmentCode = entity.getAssignmentCode();
        if (AssignmentCode != null) {
            stmt.bindString(2, AssignmentCode);
        }
 
        String TargetCode = entity.getTargetCode();
        if (TargetCode != null) {
            stmt.bindString(3, TargetCode);
        }
 
        String CompanyCode = entity.getCompanyCode();
        if (CompanyCode != null) {
            stmt.bindString(4, CompanyCode);
        }
 
        Integer AssignmentType = entity.getAssignmentType();
        if (AssignmentType != null) {
            stmt.bindLong(5, AssignmentType);
        }
        stmt.bindLong(6, entity.getStampDate().getTime());
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public SOSTargetAssignment readEntity(Cursor cursor, int offset) {
        SOSTargetAssignment entity = new SOSTargetAssignment( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // AssignmentCode
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // TargetCode
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // CompanyCode
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // AssignmentType
            new java.util.Date(cursor.getLong(offset + 5)) // StampDate
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, SOSTargetAssignment entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setAssignmentCode(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setTargetCode(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setCompanyCode(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setAssignmentType(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setStampDate(new java.util.Date(cursor.getLong(offset + 5)));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(SOSTargetAssignment entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(SOSTargetAssignment entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
