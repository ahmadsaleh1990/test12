package me.SyncWise.Android.Database;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.DaoConfig;
import de.greenrobot.dao.Property;

import me.SyncWise.Android.Database.SurveyImages;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table SurveyImages.
*/
public class SurveyImagesDao extends AbstractDao<SurveyImages, Long> {

    public static final String TABLENAME = "SurveyImages";

    /**
     * Properties of entity SurveyImages.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property SurveyID = new Property(1, long.class, "SurveyID", false, "SurveyID");
        public final static Property QuestionID = new Property(2, int.class, "QuestionID", false, "QuestionID");
        public final static Property LineID = new Property(3, int.class, "LineID", false, "LineID");
        public final static Property ImageName = new Property(4, String.class, "ImageName", false, "ImageName");
        public final static Property ImagePath = new Property(5, String.class, "ImagePath", false, "ImagePath");
        public final static Property StampDate = new Property(6, java.util.Date.class, "StampDate", false, "StampDate");
    };


    public SurveyImagesDao(DaoConfig config) {
        super(config);
    }
    
    public SurveyImagesDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'SurveyImages' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'SurveyID' INTEGER NOT NULL ," + // 1: SurveyID
                "'QuestionID' INTEGER NOT NULL ," + // 2: QuestionID
                "'LineID' INTEGER NOT NULL ," + // 3: LineID
                "'ImageName' TEXT," + // 4: ImageName
                "'ImagePath' TEXT," + // 5: ImagePath
                "'StampDate' INTEGER NOT NULL );"); // 6: StampDate
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'SurveyImages'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, SurveyImages entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getSurveyID());
        stmt.bindLong(3, entity.getQuestionID());
        stmt.bindLong(4, entity.getLineID());
 
        String ImageName = entity.getImageName();
        if (ImageName != null) {
            stmt.bindString(5, ImageName);
        }
 
        String ImagePath = entity.getImagePath();
        if (ImagePath != null) {
            stmt.bindString(6, ImagePath);
        }
        stmt.bindLong(7, entity.getStampDate().getTime());
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public SurveyImages readEntity(Cursor cursor, int offset) {
        SurveyImages entity = new SurveyImages( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // SurveyID
            cursor.getInt(offset + 2), // QuestionID
            cursor.getInt(offset + 3), // LineID
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // ImageName
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // ImagePath
            new java.util.Date(cursor.getLong(offset + 6)) // StampDate
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, SurveyImages entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setSurveyID(cursor.getLong(offset + 1));
        entity.setQuestionID(cursor.getInt(offset + 2));
        entity.setLineID(cursor.getInt(offset + 3));
        entity.setImageName(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setImagePath(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setStampDate(new java.util.Date(cursor.getLong(offset + 6)));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(SurveyImages entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(SurveyImages entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
