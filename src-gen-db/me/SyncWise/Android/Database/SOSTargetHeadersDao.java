package me.SyncWise.Android.Database;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.DaoConfig;
import de.greenrobot.dao.Property;

import me.SyncWise.Android.Database.SOSTargetHeaders;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table SOSTargetHeaders.
*/
public class SOSTargetHeadersDao extends AbstractDao<SOSTargetHeaders, Long> {

    public static final String TABLENAME = "SOSTargetHeaders";

    /**
     * Properties of entity SOSTargetHeaders.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property TargetCode = new Property(1, String.class, "TargetCode", false, "TargetCode");
        public final static Property CompanyCode = new Property(2, String.class, "CompanyCode", false, "CompanyCode");
        public final static Property TargetName = new Property(3, String.class, "TargetName", false, "TargetName");
        public final static Property TargetType = new Property(4, Integer.class, "TargetType", false, "TargetType");
        public final static Property TargetNotes = new Property(5, String.class, "TargetNotes", false, "TargetNotes");
        public final static Property StampDate = new Property(6, java.util.Date.class, "StampDate", false, "StampDate");
    };


    public SOSTargetHeadersDao(DaoConfig config) {
        super(config);
    }
    
    public SOSTargetHeadersDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'SOSTargetHeaders' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'TargetCode' TEXT," + // 1: TargetCode
                "'CompanyCode' TEXT," + // 2: CompanyCode
                "'TargetName' TEXT," + // 3: TargetName
                "'TargetType' INTEGER," + // 4: TargetType
                "'TargetNotes' TEXT," + // 5: TargetNotes
                "'StampDate' INTEGER NOT NULL );"); // 6: StampDate
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'SOSTargetHeaders'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, SOSTargetHeaders entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String TargetCode = entity.getTargetCode();
        if (TargetCode != null) {
            stmt.bindString(2, TargetCode);
        }
 
        String CompanyCode = entity.getCompanyCode();
        if (CompanyCode != null) {
            stmt.bindString(3, CompanyCode);
        }
 
        String TargetName = entity.getTargetName();
        if (TargetName != null) {
            stmt.bindString(4, TargetName);
        }
 
        Integer TargetType = entity.getTargetType();
        if (TargetType != null) {
            stmt.bindLong(5, TargetType);
        }
 
        String TargetNotes = entity.getTargetNotes();
        if (TargetNotes != null) {
            stmt.bindString(6, TargetNotes);
        }
        stmt.bindLong(7, entity.getStampDate().getTime());
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public SOSTargetHeaders readEntity(Cursor cursor, int offset) {
        SOSTargetHeaders entity = new SOSTargetHeaders( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // TargetCode
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // CompanyCode
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // TargetName
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // TargetType
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // TargetNotes
            new java.util.Date(cursor.getLong(offset + 6)) // StampDate
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, SOSTargetHeaders entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setTargetCode(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setCompanyCode(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setTargetName(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setTargetType(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setTargetNotes(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setStampDate(new java.util.Date(cursor.getLong(offset + 6)));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(SOSTargetHeaders entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(SOSTargetHeaders entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
