package me.SyncWise.Android.Database;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.DaoConfig;
import de.greenrobot.dao.Property;

import me.SyncWise.Android.Database.ShareOfShelves;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table ShareOfShelves.
*/
public class ShareOfShelvesDao extends AbstractDao<ShareOfShelves, Long> {

    public static final String TABLENAME = "ShareOfShelves";

    /**
     * Properties of entity ShareOfShelves.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property ClientCode = new Property(1, String.class, "ClientCode", false, "ClientCode");
        public final static Property CompanyCode = new Property(2, String.class, "CompanyCode", false, "CompanyCode");
        public final static Property DivisionCode = new Property(3, String.class, "DivisionCode", false, "DivisionCode");
        public final static Property BrandCode = new Property(4, String.class, "BrandCode", false, "BrandCode");
        public final static Property ShareOfShelf = new Property(5, Double.class, "ShareOfShelf", false, "ShareOfShelf");
        public final static Property ShareOfShelfCompetitor = new Property(6, Double.class, "ShareOfShelfCompetitor", false, "ShareOfShelfCompetitor");
        public final static Property Category = new Property(7, Double.class, "Category", false, "Category");
        public final static Property BDAYearlyMeter = new Property(8, Double.class, "BDAYearlyMeter", false, "BDAYearlyMeter");
        public final static Property BDAYearlyPercentage = new Property(9, Double.class, "BDAYearlyPercentage", false, "BDAYearlyPercentage");
        public final static Property StampDate = new Property(10, java.util.Date.class, "StampDate", false, "StampDate");
    };


    public ShareOfShelvesDao(DaoConfig config) {
        super(config);
    }
    
    public ShareOfShelvesDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'ShareOfShelves' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'ClientCode' TEXT NOT NULL ," + // 1: ClientCode
                "'CompanyCode' TEXT NOT NULL ," + // 2: CompanyCode
                "'DivisionCode' TEXT NOT NULL ," + // 3: DivisionCode
                "'BrandCode' TEXT NOT NULL ," + // 4: BrandCode
                "'ShareOfShelf' REAL," + // 5: ShareOfShelf
                "'ShareOfShelfCompetitor' REAL," + // 6: ShareOfShelfCompetitor
                "'Category' REAL," + // 7: Category
                "'BDAYearlyMeter' REAL," + // 8: BDAYearlyMeter
                "'BDAYearlyPercentage' REAL," + // 9: BDAYearlyPercentage
                "'StampDate' INTEGER NOT NULL );"); // 10: StampDate
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'ShareOfShelves'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, ShareOfShelves entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getClientCode());
        stmt.bindString(3, entity.getCompanyCode());
        stmt.bindString(4, entity.getDivisionCode());
        stmt.bindString(5, entity.getBrandCode());
 
        Double ShareOfShelf = entity.getShareOfShelf();
        if (ShareOfShelf != null) {
            stmt.bindDouble(6, ShareOfShelf);
        }
 
        Double ShareOfShelfCompetitor = entity.getShareOfShelfCompetitor();
        if (ShareOfShelfCompetitor != null) {
            stmt.bindDouble(7, ShareOfShelfCompetitor);
        }
 
        Double Category = entity.getCategory();
        if (Category != null) {
            stmt.bindDouble(8, Category);
        }
 
        Double BDAYearlyMeter = entity.getBDAYearlyMeter();
        if (BDAYearlyMeter != null) {
            stmt.bindDouble(9, BDAYearlyMeter);
        }
 
        Double BDAYearlyPercentage = entity.getBDAYearlyPercentage();
        if (BDAYearlyPercentage != null) {
            stmt.bindDouble(10, BDAYearlyPercentage);
        }
        stmt.bindLong(11, entity.getStampDate().getTime());
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public ShareOfShelves readEntity(Cursor cursor, int offset) {
        ShareOfShelves entity = new ShareOfShelves( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // ClientCode
            cursor.getString(offset + 2), // CompanyCode
            cursor.getString(offset + 3), // DivisionCode
            cursor.getString(offset + 4), // BrandCode
            cursor.isNull(offset + 5) ? null : cursor.getDouble(offset + 5), // ShareOfShelf
            cursor.isNull(offset + 6) ? null : cursor.getDouble(offset + 6), // ShareOfShelfCompetitor
            cursor.isNull(offset + 7) ? null : cursor.getDouble(offset + 7), // Category
            cursor.isNull(offset + 8) ? null : cursor.getDouble(offset + 8), // BDAYearlyMeter
            cursor.isNull(offset + 9) ? null : cursor.getDouble(offset + 9), // BDAYearlyPercentage
            new java.util.Date(cursor.getLong(offset + 10)) // StampDate
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, ShareOfShelves entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setClientCode(cursor.getString(offset + 1));
        entity.setCompanyCode(cursor.getString(offset + 2));
        entity.setDivisionCode(cursor.getString(offset + 3));
        entity.setBrandCode(cursor.getString(offset + 4));
        entity.setShareOfShelf(cursor.isNull(offset + 5) ? null : cursor.getDouble(offset + 5));
        entity.setShareOfShelfCompetitor(cursor.isNull(offset + 6) ? null : cursor.getDouble(offset + 6));
        entity.setCategory(cursor.isNull(offset + 7) ? null : cursor.getDouble(offset + 7));
        entity.setBDAYearlyMeter(cursor.isNull(offset + 8) ? null : cursor.getDouble(offset + 8));
        entity.setBDAYearlyPercentage(cursor.isNull(offset + 9) ? null : cursor.getDouble(offset + 9));
        entity.setStampDate(new java.util.Date(cursor.getLong(offset + 10)));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(ShareOfShelves entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(ShareOfShelves entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
