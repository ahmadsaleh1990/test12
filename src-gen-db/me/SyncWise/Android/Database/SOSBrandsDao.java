package me.SyncWise.Android.Database;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.DaoConfig;
import de.greenrobot.dao.Property;

import me.SyncWise.Android.Database.SOSBrands;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table SOSBrands.
*/
public class SOSBrandsDao extends AbstractDao<SOSBrands, Long> {

    public static final String TABLENAME = "SOSBrands";

    /**
     * Properties of entity SOSBrands.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property BrandCode = new Property(1, String.class, "BrandCode", false, "BrandCode");
        public final static Property CompanyCode = new Property(2, String.class, "CompanyCode", false, "CompanyCode");
        public final static Property BrandDescription = new Property(3, String.class, "BrandDescription", false, "BrandDescription");
        public final static Property BrandAltDescription = new Property(4, String.class, "BrandAltDescription", false, "BrandAltDescription");
        public final static Property StampDate = new Property(5, java.util.Date.class, "StampDate", false, "StampDate");
    };


    public SOSBrandsDao(DaoConfig config) {
        super(config);
    }
    
    public SOSBrandsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'SOSBrands' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'BrandCode' TEXT," + // 1: BrandCode
                "'CompanyCode' TEXT," + // 2: CompanyCode
                "'BrandDescription' TEXT," + // 3: BrandDescription
                "'BrandAltDescription' TEXT," + // 4: BrandAltDescription
                "'StampDate' INTEGER NOT NULL );"); // 5: StampDate
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'SOSBrands'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, SOSBrands entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String BrandCode = entity.getBrandCode();
        if (BrandCode != null) {
            stmt.bindString(2, BrandCode);
        }
 
        String CompanyCode = entity.getCompanyCode();
        if (CompanyCode != null) {
            stmt.bindString(3, CompanyCode);
        }
 
        String BrandDescription = entity.getBrandDescription();
        if (BrandDescription != null) {
            stmt.bindString(4, BrandDescription);
        }
 
        String BrandAltDescription = entity.getBrandAltDescription();
        if (BrandAltDescription != null) {
            stmt.bindString(5, BrandAltDescription);
        }
        stmt.bindLong(6, entity.getStampDate().getTime());
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public SOSBrands readEntity(Cursor cursor, int offset) {
        SOSBrands entity = new SOSBrands( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // BrandCode
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // CompanyCode
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // BrandDescription
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // BrandAltDescription
            new java.util.Date(cursor.getLong(offset + 5)) // StampDate
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, SOSBrands entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setBrandCode(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setCompanyCode(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setBrandDescription(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setBrandAltDescription(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setStampDate(new java.util.Date(cursor.getLong(offset + 5)));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(SOSBrands entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(SOSBrands entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
