package me.SyncWise.Android.Database;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.DaoConfig;
import de.greenrobot.dao.Property;

import me.SyncWise.Android.Database.MoneyCategory;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table MoneyCategory.
*/
public class MoneyCategoryDao extends AbstractDao<MoneyCategory, Long> {

    public static final String TABLENAME = "MoneyCategory";

    /**
     * Properties of entity MoneyCategory.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property MoneyCategoryCode = new Property(1, String.class, "MoneyCategoryCode", false, "MoneyCategoryCode");
        public final static Property MoneyCategoryDescription = new Property(2, String.class, "MoneyCategoryDescription", false, "MoneyCategoryDescription");
        public final static Property CurrencyCode = new Property(3, String.class, "CurrencyCode", false, "CurrencyCode");
        public final static Property Value = new Property(4, Integer.class, "Value", false, "Value");
        public final static Property StampDate = new Property(5, java.util.Date.class, "StampDate", false, "StampDate");
    };


    public MoneyCategoryDao(DaoConfig config) {
        super(config);
    }
    
    public MoneyCategoryDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'MoneyCategory' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'MoneyCategoryCode' TEXT NOT NULL UNIQUE ," + // 1: MoneyCategoryCode
                "'MoneyCategoryDescription' TEXT," + // 2: MoneyCategoryDescription
                "'CurrencyCode' TEXT NOT NULL ," + // 3: CurrencyCode
                "'Value' INTEGER," + // 4: Value
                "'StampDate' INTEGER NOT NULL );"); // 5: StampDate
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'MoneyCategory'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, MoneyCategory entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getMoneyCategoryCode());
 
        String MoneyCategoryDescription = entity.getMoneyCategoryDescription();
        if (MoneyCategoryDescription != null) {
            stmt.bindString(3, MoneyCategoryDescription);
        }
        stmt.bindString(4, entity.getCurrencyCode());
 
        Integer Value = entity.getValue();
        if (Value != null) {
            stmt.bindLong(5, Value);
        }
        stmt.bindLong(6, entity.getStampDate().getTime());
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public MoneyCategory readEntity(Cursor cursor, int offset) {
        MoneyCategory entity = new MoneyCategory( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // MoneyCategoryCode
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // MoneyCategoryDescription
            cursor.getString(offset + 3), // CurrencyCode
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // Value
            new java.util.Date(cursor.getLong(offset + 5)) // StampDate
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, MoneyCategory entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setMoneyCategoryCode(cursor.getString(offset + 1));
        entity.setMoneyCategoryDescription(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setCurrencyCode(cursor.getString(offset + 3));
        entity.setValue(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setStampDate(new java.util.Date(cursor.getLong(offset + 5)));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(MoneyCategory entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(MoneyCategory entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
