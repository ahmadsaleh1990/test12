package me.SyncWise.Android.Database;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.DaoConfig;
import de.greenrobot.dao.Property;

import me.SyncWise.Android.Database.Printers;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table Printers.
*/
public class PrintersDao extends AbstractDao<Printers, Long> {

    public static final String TABLENAME = "Printers";

    /**
     * Properties of entity Printers.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property PrinterMAC = new Property(1, String.class, "PrinterMAC", false, "PrinterMAC");
        public final static Property UserCode = new Property(2, String.class, "UserCode", false, "UserCode");
        public final static Property CompanyCode = new Property(3, String.class, "CompanyCode", false, "CompanyCode");
        public final static Property Type = new Property(4, int.class, "Type", false, "Type");
        public final static Property Status = new Property(5, Integer.class, "Status", false, "Status");
        public final static Property StampDate = new Property(6, java.util.Date.class, "StampDate", false, "StampDate");
    };


    public PrintersDao(DaoConfig config) {
        super(config);
    }
    
    public PrintersDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'Printers' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'PrinterMAC' TEXT NOT NULL ," + // 1: PrinterMAC
                "'UserCode' TEXT NOT NULL ," + // 2: UserCode
                "'CompanyCode' TEXT NOT NULL ," + // 3: CompanyCode
                "'Type' INTEGER NOT NULL ," + // 4: Type
                "'Status' INTEGER," + // 5: Status
                "'StampDate' INTEGER NOT NULL );"); // 6: StampDate
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'Printers'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Printers entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getPrinterMAC());
        stmt.bindString(3, entity.getUserCode());
        stmt.bindString(4, entity.getCompanyCode());
        stmt.bindLong(5, entity.getType());
 
        Integer Status = entity.getStatus();
        if (Status != null) {
            stmt.bindLong(6, Status);
        }
        stmt.bindLong(7, entity.getStampDate().getTime());
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public Printers readEntity(Cursor cursor, int offset) {
        Printers entity = new Printers( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // PrinterMAC
            cursor.getString(offset + 2), // UserCode
            cursor.getString(offset + 3), // CompanyCode
            cursor.getInt(offset + 4), // Type
            cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5), // Status
            new java.util.Date(cursor.getLong(offset + 6)) // StampDate
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Printers entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setPrinterMAC(cursor.getString(offset + 1));
        entity.setUserCode(cursor.getString(offset + 2));
        entity.setCompanyCode(cursor.getString(offset + 3));
        entity.setType(cursor.getInt(offset + 4));
        entity.setStatus(cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5));
        entity.setStampDate(new java.util.Date(cursor.getLong(offset + 6)));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Printers entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(Printers entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
