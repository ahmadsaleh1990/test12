package me.SyncWise.Android.Database;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.DaoConfig;
import de.greenrobot.dao.Property;

import me.SyncWise.Android.Database.Permissions;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table Permissions.
*/
public class PermissionsDao extends AbstractDao<Permissions, Long> {

    public static final String TABLENAME = "Permissions";

    /**
     * Properties of entity Permissions.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property PermissionID = new Property(1, int.class, "PermissionID", false, "PermissionID");
        public final static Property PermissionName = new Property(2, String.class, "PermissionName", false, "PermissionName");
        public final static Property PermissionAltName = new Property(3, String.class, "PermissionAltName", false, "PermissionAltName");
        public final static Property PermissionDefaultValue = new Property(4, String.class, "PermissionDefaultValue", false, "PermissionDefaultValue");
        public final static Property PermissionTypeID = new Property(5, Integer.class, "PermissionTypeID", false, "PermissionTypeID");
        public final static Property StampDate = new Property(6, java.util.Date.class, "StampDate", false, "StampDate");
    };


    public PermissionsDao(DaoConfig config) {
        super(config);
    }
    
    public PermissionsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'Permissions' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'PermissionID' INTEGER NOT NULL UNIQUE ," + // 1: PermissionID
                "'PermissionName' TEXT," + // 2: PermissionName
                "'PermissionAltName' TEXT," + // 3: PermissionAltName
                "'PermissionDefaultValue' TEXT," + // 4: PermissionDefaultValue
                "'PermissionTypeID' INTEGER," + // 5: PermissionTypeID
                "'StampDate' INTEGER NOT NULL );"); // 6: StampDate
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'Permissions'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Permissions entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getPermissionID());
 
        String PermissionName = entity.getPermissionName();
        if (PermissionName != null) {
            stmt.bindString(3, PermissionName);
        }
 
        String PermissionAltName = entity.getPermissionAltName();
        if (PermissionAltName != null) {
            stmt.bindString(4, PermissionAltName);
        }
 
        String PermissionDefaultValue = entity.getPermissionDefaultValue();
        if (PermissionDefaultValue != null) {
            stmt.bindString(5, PermissionDefaultValue);
        }
 
        Integer PermissionTypeID = entity.getPermissionTypeID();
        if (PermissionTypeID != null) {
            stmt.bindLong(6, PermissionTypeID);
        }
        stmt.bindLong(7, entity.getStampDate().getTime());
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public Permissions readEntity(Cursor cursor, int offset) {
        Permissions entity = new Permissions( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getInt(offset + 1), // PermissionID
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // PermissionName
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // PermissionAltName
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // PermissionDefaultValue
            cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5), // PermissionTypeID
            new java.util.Date(cursor.getLong(offset + 6)) // StampDate
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Permissions entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setPermissionID(cursor.getInt(offset + 1));
        entity.setPermissionName(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setPermissionAltName(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setPermissionDefaultValue(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setPermissionTypeID(cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5));
        entity.setStampDate(new java.util.Date(cursor.getLong(offset + 6)));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Permissions entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(Permissions entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
