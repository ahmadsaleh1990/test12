package me.SyncWise.Android.Database;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.DaoConfig;
import de.greenrobot.dao.Property;

import me.SyncWise.Android.Database.PromotionAssignments;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table PromotionAssignments.
*/
public class PromotionAssignmentsDao extends AbstractDao<PromotionAssignments, Long> {

    public static final String TABLENAME = "PromotionAssignments";

    /**
     * Properties of entity PromotionAssignments.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property AssignmentCode = new Property(1, String.class, "AssignmentCode", false, "AssignmentCode");
        public final static Property PromotionID = new Property(2, int.class, "PromotionID", false, "PromotionID");
        public final static Property AssignmentType = new Property(3, Integer.class, "AssignmentType", false, "AssignmentType");
        public final static Property StampDate = new Property(4, java.util.Date.class, "StampDate", false, "StampDate");
    };


    public PromotionAssignmentsDao(DaoConfig config) {
        super(config);
    }
    
    public PromotionAssignmentsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'PromotionAssignments' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'AssignmentCode' TEXT NOT NULL ," + // 1: AssignmentCode
                "'PromotionID' INTEGER NOT NULL ," + // 2: PromotionID
                "'AssignmentType' INTEGER," + // 3: AssignmentType
                "'StampDate' INTEGER NOT NULL );"); // 4: StampDate
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'PromotionAssignments'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, PromotionAssignments entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getAssignmentCode());
        stmt.bindLong(3, entity.getPromotionID());
 
        Integer AssignmentType = entity.getAssignmentType();
        if (AssignmentType != null) {
            stmt.bindLong(4, AssignmentType);
        }
        stmt.bindLong(5, entity.getStampDate().getTime());
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public PromotionAssignments readEntity(Cursor cursor, int offset) {
        PromotionAssignments entity = new PromotionAssignments( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // AssignmentCode
            cursor.getInt(offset + 2), // PromotionID
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // AssignmentType
            new java.util.Date(cursor.getLong(offset + 4)) // StampDate
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, PromotionAssignments entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setAssignmentCode(cursor.getString(offset + 1));
        entity.setPromotionID(cursor.getInt(offset + 2));
        entity.setAssignmentType(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setStampDate(new java.util.Date(cursor.getLong(offset + 4)));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(PromotionAssignments entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(PromotionAssignments entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
