package me.SyncWise.Android.Database;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.DaoConfig;
import de.greenrobot.dao.Property;

import me.SyncWise.Android.Database.UserAccountNumbers;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table UserAccountNumbers.
*/
public class UserAccountNumbersDao extends AbstractDao<UserAccountNumbers, Long> {

    public static final String TABLENAME = "UserAccountNumbers";

    /**
     * Properties of entity UserAccountNumbers.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property UserCode = new Property(1, String.class, "UserCode", false, "UserCode");
        public final static Property CompanyCode = new Property(2, String.class, "CompanyCode", false, "CompanyCode");
        public final static Property LineID = new Property(3, int.class, "LineID", false, "LineID");
        public final static Property AccountType = new Property(4, Integer.class, "AccountType", false, "AccountType");
        public final static Property AccountNumber = new Property(5, String.class, "AccountNumber", false, "AccountNumber");
        public final static Property DivisionCode = new Property(6, String.class, "DivisionCode", false, "DivisionCode");
        public final static Property StampDate = new Property(7, java.util.Date.class, "StampDate", false, "StampDate");
    };


    public UserAccountNumbersDao(DaoConfig config) {
        super(config);
    }
    
    public UserAccountNumbersDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'UserAccountNumbers' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'UserCode' TEXT NOT NULL ," + // 1: UserCode
                "'CompanyCode' TEXT NOT NULL ," + // 2: CompanyCode
                "'LineID' INTEGER NOT NULL ," + // 3: LineID
                "'AccountType' INTEGER," + // 4: AccountType
                "'AccountNumber' TEXT," + // 5: AccountNumber
                "'DivisionCode' TEXT," + // 6: DivisionCode
                "'StampDate' INTEGER NOT NULL );"); // 7: StampDate
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'UserAccountNumbers'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, UserAccountNumbers entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getUserCode());
        stmt.bindString(3, entity.getCompanyCode());
        stmt.bindLong(4, entity.getLineID());
 
        Integer AccountType = entity.getAccountType();
        if (AccountType != null) {
            stmt.bindLong(5, AccountType);
        }
 
        String AccountNumber = entity.getAccountNumber();
        if (AccountNumber != null) {
            stmt.bindString(6, AccountNumber);
        }
 
        String DivisionCode = entity.getDivisionCode();
        if (DivisionCode != null) {
            stmt.bindString(7, DivisionCode);
        }
        stmt.bindLong(8, entity.getStampDate().getTime());
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public UserAccountNumbers readEntity(Cursor cursor, int offset) {
        UserAccountNumbers entity = new UserAccountNumbers( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // UserCode
            cursor.getString(offset + 2), // CompanyCode
            cursor.getInt(offset + 3), // LineID
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // AccountType
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // AccountNumber
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // DivisionCode
            new java.util.Date(cursor.getLong(offset + 7)) // StampDate
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, UserAccountNumbers entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUserCode(cursor.getString(offset + 1));
        entity.setCompanyCode(cursor.getString(offset + 2));
        entity.setLineID(cursor.getInt(offset + 3));
        entity.setAccountType(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setAccountNumber(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setDivisionCode(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setStampDate(new java.util.Date(cursor.getLong(offset + 7)));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(UserAccountNumbers entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(UserAccountNumbers entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
